# gjevt

    go install github.com/leftmike/gjevt@latest

Calls [Jev](https://openrouter.ai/typesafe) through OpenRouter's decisions API.
On first run it creates `./gjevt.hcl`; set `api_key` there.

    gjevt examples/support.json
